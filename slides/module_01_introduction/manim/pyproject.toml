[project]
name = "module-01-manim"
version = "0.1.0"
description = "Manim animations for Module 1: Course Introduction"
requires-python = ">=3.10"
dependencies = [
    "manim>=0.18.0",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
