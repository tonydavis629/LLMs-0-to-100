[project]
name = "module-02-manim"
version = "0.1.0"
description = "Manim animations for Module 2: Perceptrons and Optimization"
requires-python = ">=3.10"
dependencies = [
    "manim>=0.18.0",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
