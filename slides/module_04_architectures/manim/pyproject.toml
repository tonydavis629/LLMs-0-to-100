[project]
name = "module-04-manim"
version = "0.1.0"
description = "Manim animations for Module 4: LLM Architectures"
requires-python = ">=3.10"
dependencies = [
    "manim>=0.18.0",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
