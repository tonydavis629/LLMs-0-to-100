(function () {
  // Module 12 config. This deck has no Manim clips: the comparisons in this
  // module are tables, cost curves, and sequential diagrams, which reveal.js
  // fragments and the shared card/table styles handle without spatial
  // animation. The one real data visualization, the log-log cost plot, is a
  // matplotlib figure produced by the exercise itself.
  window.MODULE_CONFIG = {
    title: 'LLMs 0 to 100 - Module 12',
    manimSections: {},
    widgets: {}
  };
}());
